@model IEnumerable<MovimentosManuaisTeste.Models.prSelecionaProducoCosif_Result>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.COD_COSIF)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COD_CLASSIFICACAO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STA_STATUS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.COD_COSIF)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.COD_CLASSIFICACAO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STA_STATUS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_PRODUTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_PRODUTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_PRODUTO })
        </td>
    </tr>
}

</table>
